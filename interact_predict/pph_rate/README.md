Each file in `pph_rate/` provides **a non-interaction plot** for the predictor variable `pph_rate`, visualizing its independent effect without considering interactions with other predictors. This helps assess its standalone impact on model performance.

#### Files in Folder
- `pph_non_interact.png` – A model performance visualization **without interaction terms**.