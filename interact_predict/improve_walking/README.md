Each file in `transfer_to_provider/` contains **multiple interaction plots** for the predictor variable `transfer_to_provider`, visualizing its relationships with other predictors and identifying potential **interaction effects** that may influence model performance.

#### Files in Folder

- `walk_no_interact.png` – Model performance visualization **without interaction terms**.
- `walk_quality.png` – Model performance visualization **with interaction terms** between `improve_walking` and care quality.  
- `walk_performance.png` – Model performance visualization **with interaction terms** between `improve_walking` and overall performance metrics.  
- `walk_improve.png` – Model performance visualization **with interaction terms** between `improve_walking` and patient improvement outcomes.  
- `walk_transfer.png` – Model performance visualization **with interaction terms** between `improve_walking` and information transfer metrics.  
- `walk_finance.png` – Model performance visualization **with interaction terms** between `improve_walking` and financial metrics.

