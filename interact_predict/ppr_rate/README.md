Each file in `ppr_rate/` provides **an interaction plot** for the predictor variable `ppr_rate`, visualizing its relationship with other predictors and identifying potential **interaction effects** that may impact model performance.

#### Files in Folder

- `ppr_interact.png` – A model performance visualization **with interaction terms**.
